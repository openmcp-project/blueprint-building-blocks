# The Chart.yaml file is required for a chart. See all avaiable fields: https://helm.sh/docs/topics/charts/#the-chartyaml-file
apiVersion: v2
name: crossplane-provider-pkgs
description: A Helm Chart to template crossplane provider "kind Provider" manifests to install crossplane family providers.
# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application
# A URL to an SVG or PNG image to be used as an icon
icon: "https://avatars.githubusercontent.com/u/45158470?s=48&v=4"
# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.0.15
# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.20.0"
# The URL of this projects home page (optional)
home: "https://github.com/openmcp-project/blueprints"
# A list of URLs to source code for this project (optional)
sources:
  # Source repository for all Crossplane core package kinds
  - https://github.com/crossplane/crossplane
  # pkg.crossplane.io/v1 Provider
  - https://doc.crds.dev/github.com/crossplane/crossplane/pkg.crossplane.io/Provider/v1
  # pkg.crossplane.io/v1 Configuration
  - https://doc.crds.dev/github.com/crossplane/crossplane/pkg.crossplane.io/Configuration/v1
  # pkg.crossplane.io/v1beta1 Function (template uses v1beta1; CRD promoted to v1 in Crossplane v1.14+)
  - https://doc.crds.dev/github.com/crossplane/crossplane/pkg.crossplane.io/Function/v1
  # pkg.crossplane.io/v1beta1 DeploymentRuntimeConfig
  - https://doc.crds.dev/github.com/crossplane/crossplane/pkg.crossplane.io/DeploymentRuntimeConfig/v1beta1
  # pkg.crossplane.io/v1alpha1 ImageConfig (template uses v1alpha1; CRD promoted to v1beta1 in Crossplane v2.x)
  - https://doc.crds.dev/github.com/crossplane/crossplane/pkg.crossplane.io/ImageConfig/v1beta1
# Whether this chart is deprecated (optional, boolean)
deprecated: false
