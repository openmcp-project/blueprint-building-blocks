{{- range $item := .Values.passwords}}
  {{- if and ($item) (ne $item.name "")}}
---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: {{required "A valid value is required! (.Values.passwords[].name)" $item.name | lower | quote}}
  namespace: {{ $item.namespace | default $.Values.defaults.namespace | lower | quote }}
  labels:
    openmcp.cloud/blueprint-building-block: "{{ $.Chart.Name }}"
    openmcp.cloud/blueprint-building-block-version: "{{ $.Chart.Version }}"
spec:
  {{- required "A valid value is required! (.Values.passwords[].spec)" $item.spec | toYaml | nindent 2 }}
  {{- end }}
{{- end }}
