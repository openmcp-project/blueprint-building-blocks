---
# The Chart.yaml file is required for a chart. See all avaiable fields: https://helm.sh/docs/topics/charts/#the-chartyaml-file
apiVersion: v2
name: flux-config
description: A Helm Chart to template flux manifests to leverage GitOps on a OpenMCP cluster.
# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application
# A URL to an SVG or PNG image to be used as an icon
icon: "https://avatars.githubusercontent.com/u/52158677?s=200&v=4"
# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.0.22
# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "2.6.0"
# The URL of this projects home page (optional)
home: "https://github.com/openmcp-project/blueprints"
# A list of URLs to source code for this project (optional)
sources:
  # flux2 umbrella project
  - https://github.com/fluxcd/flux2
  # source.toolkit.fluxcd.io — source-controller
  - https://github.com/fluxcd/source-controller
  - https://doc.crds.dev/github.com/fluxcd/source-controller/source.toolkit.fluxcd.io/GitRepository/v1
  - https://doc.crds.dev/github.com/fluxcd/source-controller/source.toolkit.fluxcd.io/HelmRepository/v1
  - https://doc.crds.dev/github.com/fluxcd/source-controller/source.toolkit.fluxcd.io/OCIRepository/v1
  # kustomize.toolkit.fluxcd.io — kustomize-controller
  - https://github.com/fluxcd/kustomize-controller
  - https://doc.crds.dev/github.com/fluxcd/kustomize-controller/kustomize.toolkit.fluxcd.io/Kustomization/v1
  # helm.toolkit.fluxcd.io — helm-controller
  - https://github.com/fluxcd/helm-controller
  - https://doc.crds.dev/github.com/fluxcd/helm-controller/helm.toolkit.fluxcd.io/HelmRelease/v2
# Whether this chart is deprecated (optional, boolean)
deprecated: false
