{{- range $item := .Values.imageUpdateAutomations}}
  {{- if and ($item) (ne $item.name "")}}
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: {{required "A valid value is required! (.Values.imageUpdateAutomations[].name)" $item.name | lower | quote}}
  namespace: {{ $item.namespace | default $.Values.defaults.namespace | lower | quote }}
  labels:
    openmcp.cloud/blueprint-building-block: "{{ $.Chart.Name }}"
    openmcp.cloud/blueprint-building-block-version: "{{ $.Chart.Version }}"
spec:
  {{- toYaml $item.spec | nindent 2 }}
  {{- end }}
{{- end }}
